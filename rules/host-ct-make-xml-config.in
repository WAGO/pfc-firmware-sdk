## SECTION=wago_specific_config

config HOST_CT_MAKE_XML_CONFIG
	bool
	default n
	help
	 A kconfig- and generateDS.py-based tool used to generate XML menu
	 definitions for configuration frontends (wbm, ipcconfig etc.) via ptxdist
	 menuconfig
