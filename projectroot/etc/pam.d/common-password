#
# /etc/pam.d/common-password; Linux-PAM configuration file
#

password [success=1 default=ignore]                       pam_unix.so   sha512
password requisite                                        pam_deny.so
password required                                         pam_permit.so

