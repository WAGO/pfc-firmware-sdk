# Auto-generated file: USE WBM/CBM TO CHANGE THESE SETTINGS!

# This script is not evaluated by pureftpd directly.
# It is sourced inside the start script with the command line parameters
# set accordingly.

#
# 0: SSL/TLS disabled
# 1: SSL/TLS active, connections over ftp and ftps accepted
# 2: SSL/TLS active, only ftps connections accepted
TLS=2
