## SECTION=image2

config IMAGE_EMMC
	tristate
	select HOST_GENIMAGE
	select IMAGE_ROOT_EXT

#   # Required because cmdline file is created there
#   # TODO: make cmdline a separate target?
    select IMAGE_BOOT_MLO

	prompt "Generate images/emmc.hdimg"
	help
	  FIXME
